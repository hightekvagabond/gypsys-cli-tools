# SYSTEM Configuration
# System-wide configuration for modular monitoring suite
# This file contains defaults used by orchestrator and status.sh

# ========================================
# ORCHESTRATOR SETTINGS
# ========================================

# Default monitoring interval when running all modules (seconds)
DEFAULT_MONITOR_INTERVAL=300  # 5 minutes

# Modules directory path (relative to project root)
MODULES_DIR="modules"

# State directory for persistent data
STATE_DIR="/var/tmp/modular-monitor-state"

# ========================================
# STATUS SCRIPT SETTINGS  
# ========================================

# Default time span for status analysis if not specified
DEFAULT_STATUS_TIMESPAN="1 hour ago"

# Default end time for status analysis
DEFAULT_STATUS_END_TIME="now"

# Status display options
STATUS_SHOW_CURRENT_READINGS=true
STATUS_SHOW_MODULE_TESTS=true
STATUS_SHOW_RECENT_ALERTS=true
STATUS_SHOW_SYSTEM_INFO=true

# ========================================
# LOGGING & ALERTING
# ========================================

# Global log level (debug, info, warning, error)
# Note: Individual modules now have their own LOG_LEVEL settings in config.conf
# This serves as a fallback for modules that don't specify their own log level
GLOBAL_LOG_LEVEL="info"

# Global alert settings
# Note: Individual modules now have their own notification settings in config.conf
# These serve as fallbacks for modules that don't specify their own settings
ENABLE_GLOBAL_NOTIFICATIONS=true
GLOBAL_NOTIFICATION_TIMEOUT=10000  # milliseconds

# Emergency response settings
ENABLE_EMERGENCY_ACTIONS=true

# ========================================
# MODULE MANAGEMENT
# ========================================

# Path to enabled modules (symlinks)
ENABLED_MODULES_DIR="config"

# Module override configs directory
MODULE_OVERRIDES_DIR="config"

# ========================================
# HARDWARE DETECTION & SYSTEM INFO
# ========================================

# Auto-detect hardware and enable appropriate modules
AUTO_DETECT_HARDWARE=true

# Hardware-specific settings (detected during install)
HARDWARE_TYPE="predator"  # Will be set by installer: "predator", "generic", etc.
CPU_VENDOR="intel"     # Will be set by installer: "intel", "amd", etc.
GPU_VENDOR="intel"     # Will be set by installer: "intel", "nvidia", "amd", etc.

# ========================================
# INSTALLATION SETTINGS
# ========================================

# Whether this is the first run after installation
FIRST_RUN_AFTER_INSTALL=false

# Modules that need to run during installation
# These will be identified by RUN_ON_INSTALL=true in their configs
RUN_ON_INSTALL_MODULES_DETECTED=true

# ========================================
# PERFORMANCE & RESOURCE LIMITS
# ========================================

# Maximum number of modules to run concurrently
MAX_CONCURRENT_MODULES=3

# Timeout for individual module execution (seconds)
MODULE_EXECUTION_TIMEOUT=60

# ========================================
# DEBUG & DEVELOPMENT
# ========================================

# Global debug mode
DEBUG_MODE=false

# Verbose logging for troubleshooting
VERBOSE_LOGGING=false

# Test mode (dry run without actual actions)
TEST_MODE=false

# ========================================
# BACKWARDS COMPATIBILITY
# ========================================

# Support for legacy configuration files
LEGACY_CONFIG_SUPPORT=true

# Path to legacy framework config
LEGACY_FRAMEWORK_CONFIG="framework/monitor-config.sh"

# Path to legacy thresholds config
LEGACY_THRESHOLDS_CONFIG="config/thresholds.conf"

# ========================================
# NOTES
# ========================================

# This file is sourced by:
# - monitor.sh (for module management and execution)
# - status.sh (for display preferences and defaults)
# - setup.sh (for system detection and configuration)
#
# Module-specific overrides should be placed in individual
# config files in the config/ directory following the pattern:
# MODULE_NAME.conf (e.g., thermal.conf, i915.conf)
#
# The installer will detect hardware and update the hardware
# detection variables above during initial setup.
