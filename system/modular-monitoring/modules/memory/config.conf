# Memory Monitor Configuration
# Default configuration for memory monitoring module

# Memory thresholds (percentage)
MEMORY_WARNING=85
MEMORY_CRITICAL=95

# Memory check interval (seconds)
MEMORY_CHECK_INTERVAL=15

# Alert cooldowns (seconds)
WARNING_COOLDOWN=300      # 5 minutes - memory warnings need monitoring
CRITICAL_COOLDOWN=120     # 2 minutes - memory critical can cause crashes
EMERGENCY_COOLDOWN=45     # 45 seconds - memory emergency needs quick response

# Desktop notifications
ENABLE_DESKTOP_NOTIFICATIONS=true
NOTIFICATION_TIMEOUT=10000  # milliseconds

# Logging
LOG_LEVEL=info  # debug, info, warning, error
DEBUG_MODE=false

# Autofix settings
ENABLE_MEMORY_AUTOFIX=true
MEMORY_AUTO_CLEANUP=true       # Automatically trigger memory cleanup when possible

# Status script settings
DEFAULT_STATUS_START_TIME="1 hour ago"
DEFAULT_STATUS_END_TIME="now"

# Run on install flag
RUN_ON_INSTALL=false
RUN_ON_INSTALL_WARNING="This module may take a while to run initially"

# Memory autofix settings (moved from global autofix.conf)
MEMORY_KILL_THRESHOLD=15          # Minimum memory % to consider for emergency killing  
ENABLE_CACHE_DROP=true            # Allow dropping system caches to free memory
ENABLE_EMERGENCY_MEMORY_KILL=false  # Allow killing memory-heavy processes (dangerous!)
SWAP_WARNING_THRESHOLD=50         # Swap usage % to trigger warnings

# Emergency process kill settings (moved from global autofix.conf)
PROCESS_CPU_THRESHOLD=10          # Minimum CPU % to consider for emergency killing
KILL_PROCESS_WAIT_TIME=2          # Seconds to wait after SIGTERM before SIGKILL

# Autofix grace period settings (moved from global autofix.conf)
MEMORY_AUTOFIX_GRACE_SECONDS=30   # Grace period for memory autofix actions
GRACE_PERIOD_SECONDS=60           # General grace period after system/process start

# Maximum autofix actions per hour per script (from autofix.conf)
MAX_AUTOFIXES_PER_HOUR=5

# Minimum time between identical autofix actions (from autofix.conf)
AUTOFIX_COOLDOWN_MINUTES=10

# Require user confirmation for dangerous actions (from autofix.conf)
REQUIRE_CONFIRMATION=false

# Enable notifications for this module (from SYSTEM.conf ENABLE_GLOBAL_NOTIFICATIONS)
ENABLE_NOTIFICATIONS=true
